import 'package:flutter/material.dart';

class MedicinesStockScreen extends StatelessWidget {
  const MedicinesStockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Medicines Stock Screen'),
      ),
    );
  }
}
